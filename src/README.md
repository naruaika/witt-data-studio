# Witt

This directory contains entry point source code for the application.

```txt
. (Application::Adw.Application)
├── window (Window::Adw.ApplicationWindow)
└── window (Window::Adw.ApplicationWindow)
```